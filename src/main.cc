#include "../include/chain.h"

int main(void)
{
  Chain *c = new Chain();

  // while (c->get_size() < 2) {
  //   c->generate_next_block("something");
  // }

  // std::cout << c->get_size() << std::endl;
  // c->print_chain();
}